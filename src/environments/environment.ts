export const environment = {
  production: false,
  // When the real backend exists: set this to its base URL and remove the
  // mockApiInterceptor from app.config.ts. Nothing else changes.
  apiBase: '/api',
  useMock: true,
};
